INTERFACE
 SUBROUTINE UKCA_PRIM_SS(KIDIA,KFDIA,KBOX,PSURTP,KSSEMS, PLAND_FRAC,&
 & PSEAICE,PSM,PUS10M,PSST,PNOT,PAIRD,&
 & PBUD_AER_MAS_PRIMSS,KVERBOSE,&
 & PBUD_AER_NUM_PRIMSS) 
 USE PARKIND1 ,ONLY : JPIM ,JPRB
 USE UKCA_MODE_SETUP, ONLY: NMODES, CP_CL, NCP, DDPLIM0, MODE,&
 & COMPONENT, MM, RHOCOMP 
 INTEGER(KIND=JPIM), INTENT(IN) :: KIDIA, KFDIA, KBOX, KSSEMS
 INTEGER(KIND=JPIM), INTENT(IN) :: KVERBOSE
 REAL(KIND=JPRB), INTENT(IN) :: PSURTP(KBOX)
 REAL(KIND=JPRB), INTENT(IN) :: PSM(KBOX)
 REAL(KIND=JPRB), INTENT(IN) :: PLAND_FRAC(KBOX)
 REAL(KIND=JPRB), INTENT(IN) :: PSEAICE(KBOX)
 REAL(KIND=JPRB), INTENT(IN) :: PSST(KBOX)
 REAL(KIND=JPRB), INTENT(IN) :: PUS10M(KBOX)
 REAL(KIND=JPRB), INTENT(IN) :: PNOT(KBOX)
 REAL(KIND=JPRB), INTENT(IN) :: PAIRD(KBOX)
 REAL(KIND=JPRB), INTENT(OUT) :: PBUD_AER_MAS_PRIMSS(KBOX,NMODES)
 REAL(KIND=JPRB), INTENT(OUT) :: PBUD_AER_NUM_PRIMSS(KBOX,NMODES)
 END SUBROUTINE UKCA_PRIM_SS
END INTERFACE
