INTERFACE
SUBROUTINE AOD_GLOMAP_CONVERT_MDND&
 & (KLEN, KFLEVG, PNPM, PCPMMR, LDMSOL, PT, PPF, PQF, PRH, PAIRD,&
 & PWET_DIAM, PDRY_DIAM, PCOMP_VOL, PMODAL_WTV, PMODAL_RHO, PMODAL_VOL) 
USE PARKIND1 , ONLY : JPIM, JPRB
USE UKCA_MODE_SETUP, ONLY: NMODES, NCP, MM, MFRAC_0, MMID, MODE, COMPONENT, NUM_EPS
INTEGER(KIND=JPIM), INTENT(IN) :: KLEN, KFLEVG
REAL(KIND=JPRB), INTENT(IN) :: PNPM(KLEN,KFLEVG,NMODES)
REAL(KIND=JPRB), INTENT(IN) :: PCPMMR(KLEN,KFLEVG,NMODES,NCP)
REAL(KIND=JPRB), INTENT(IN) :: PT(KLEN,KFLEVG)
REAL(KIND=JPRB), INTENT(IN) :: PPF(KLEN,KFLEVG)
REAL(KIND=JPRB), INTENT(IN) :: PQF(KLEN,KFLEVG)
REAL(KIND=JPRB), INTENT(IN) :: PRH(KLEN,KFLEVG)
REAL(KIND=JPRB), INTENT(IN) :: PAIRD(KLEN,KFLEVG)
LOGICAL, INTENT(IN) :: LDMSOL(NMODES)
REAL(KIND=JPRB), INTENT(OUT) :: PWET_DIAM(KLEN,KFLEVG,NMODES)
REAL(KIND=JPRB), INTENT(OUT) :: PDRY_DIAM(KLEN,KFLEVG,NMODES)
REAL(KIND=JPRB), INTENT(OUT) :: PCOMP_VOL(KLEN,KFLEVG,NMODES,NCP)
REAL(KIND=JPRB), INTENT(OUT) :: PMODAL_WTV(KLEN,KFLEVG,NMODES)
REAL(KIND=JPRB), INTENT(OUT) :: PMODAL_RHO(KLEN,KFLEVG,NMODES)
REAL(KIND=JPRB), INTENT(OUT) :: PMODAL_VOL(KLEN,KFLEVG,NMODES)
END SUBROUTINE AOD_GLOMAP_CONVERT_MDND
END INTERFACE
