INTERFACE
SUBROUTINE RTTOV_US76_PROF(ERR, IPROF, NLEVELS, PROFILE)
  USE PARKIND1, ONLY : JPRB, JPIM
  USE RTTOV_TYPES, ONLY : RTTOV_PROFILE
  IMPLICIT NONE
  INTEGER(KIND=JPIM),  INTENT(OUT)   :: ERR
  INTEGER(KIND=JPIM),  INTENT(IN)    :: IPROF, NLEVELS
  TYPE(RTTOV_PROFILE), INTENT(INOUT) :: PROFILE
END SUBROUTINE
END INTERFACE
